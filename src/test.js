var TestCase = {
	loadTest : function(){
		this.testFormat();
	},
	testFormat : function(){
		//cc.log(GameTool.num2strFix2(9));
		//cc.log(GameTool.num2strFix2(15));

		// var str = "I Love {0}, and You Love {1},Where are {0}! {4}";

		// alert(str.format("You","Me"));
		
		
		//        var t = expand("这是测试@0, 字符串@1.", "abc", 0);
       // alert(t);

        // var fish = new Fish();
        // fish.print();

        // var fish1 = new FishOne();
        // fish1.print();
        // fish1.test();
		
		// var q = new Queue();
		// var i = 0;
		// for(i = 0; i < 100; i++){
		// 	q.push(i);
		// }
		// for(i = 0; i < 50; i++){
		// 	cc.log(q.pop());
		// }
		
		// cc.log("-----------");
		// for(i = 0; i < 10; i++){
		// 	cc.log(q.peek());
		// }
		// cc.log("-----------");
		// for(i = 0; i < 10; i++){
		// 	q.push(i);
		// }
		// cc.log("left:" + q.size());
		// var len = q.size();
		// for(i = 0; i < len; i++){
		// 	cc.log("i=" + i + "\t" + q.pop());
		// }
		// cc.log("end" + q.size());
	},
};
